# jarvisd production configuration (docs/09 §1, F8.9).
#
# Copy to /etc/jarvis/jarvisd.toml. Environment overrides file values
# (`JARVIS__SERVER__BIND=…`), which is how a systemd drop-in changes one
# setting without editing this file.
#
# Two rules run through the whole file:
#
#   * SECRETS ARE REFERENCES, never literals (invariant 5). Every `*_secret`
#     and `*_ref` takes `keyring:service/entry` or `env:VAR`. A literal here is
#     refused at startup rather than at first use.
#   * OUTBOUND CAPABILITIES ARE OFF UNTIL NAMED. Voice, web search, Home
#     Assistant, media, maps — each stays unregistered until this file says
#     otherwise. A fresh install talks to nothing.

[server]
# LOOPBACK BY DEFAULT, and the third rule of this file: a fresh install is not
# reachable from the network until you say so.
#
# jarvisd refuses to bind anything but loopback without [server.tls] below
# (docs/06 §7) — it will not serve device tokens in the clear on a LAN. So the
# two settings move together, and the packaged default is the pair that works
# with nothing else installed: loopback, plaintext, `http://127.0.0.1:8741/`.
#
# To let satellites reach this daemon over the LAN, do both, in this order:
#
#   sudo infra/install/generate-tls-cert.sh      # writes /var/lib/jarvis/tls
#   then set bind = "0.0.0.0:8741" and uncomment [server.tls] below.
#
# The order matters: with 0.0.0.0 + [server.tls] pointing at a certificate that
# is not there yet, the daemon fail-fasts at startup rather than binding — by
# design, since the alternative is a listener whose identity nodes cannot pin.
bind = "127.0.0.1:8741"

# Static Angular assets. install.sh rewrites this line to the installed path
# (/var/lib/jarvis/web) — it MUST stay inside this [server] table: a second
# `[server]` header later in the file is invalid TOML and the daemon refuses to
# load at all.
# web_assets = "/var/lib/jarvis/web"

# [server.tls]
# Generate with infra/install/generate-tls-cert.sh. Self-signed and PINNED by
# each node at pairing (ADR-031 §4) — regenerating these invalidates every
# paired node, which is the correct behaviour and a real inconvenience, so do
# it deliberately.
# cert_path = "/var/lib/jarvis/tls/cert.pem"
# key_path = "/var/lib/jarvis/tls/key.pem"

[database]
# NOT a keyring reference, and the reason is load-bearing (F10.9).
#
# jarvisd runs as a systemd SYSTEM service: User=jarvis, ProtectHome=true, no
# login session and therefore no D-Bus session bus. keyring's Secret Service
# backend has nothing to talk to, the lookup fails, and an unresolvable secret
# reference is a fatal config error — the daemon would not start at all.
#
# install.sh writes JARVIS_DB_URL into /etc/jarvis/secrets.env (root-only,
# mode 0600) and jarvisd.service reads it with EnvironmentFile=. systemd reads
# that file as root BEFORE dropping to User=jarvis, so the service account
# never needs read access, and the value never appears in a command line
# (invariant 5).
#
# The keyring remains correct for jarvis-agent, which is a USER unit inside a
# graphical session, and for interactively-provisioned secrets below.
url_secret = "env:JARVIS_DB_URL"
max_connections = 8

# --- voice ----------------------------------------------------------------
[voice]
enabled = true
# faster-whisper base int8 (decided at F8.9; see infra/compose/voice.yml for
# why base rather than small).
wyoming_stt = "tcp://127.0.0.1:10300"
# Piper. This is the LOCAL voice and it stays the fallback even with a cloud
# voice enabled below — an alarm must sound with the network down (ADR-023).
wyoming_tts = "tcp://127.0.0.1:10200"

# The word satellites answer to (ADR-032 §1). Configuration, not code: changing
# it is this setting plus a model, never a rebuild. It can also be changed from
# Settings -> Voice in the shell, which stores an override that wins over this
# value.
#
# It MUST be a word you have a model for. openWakeWord publishes six:
#   alexa, hey jarvis, hey mycroft, hey rhasspy, timer, weather
# Anything else needs a model trained for it — openWakeWord's own pipeline
# synthesises the training clips from TTS, so it needs no recordings of your
# voice, but it does need a GPU session. A node configured for a word it has no
# model for says so at startup and falls back to push-to-talk; it never fails to
# boot, and it never silently goes deaf.
wake_word = "hey jarvis"

# What the settings surface offers, i.e. the models you have actually installed
# on your satellites with infra/install/fetch-wake-assets.sh.
#
# Declared rather than discovered: the models live on the nodes and the daemon
# cannot see their filesystems. A word missing from this list cannot be chosen
# from the UI, which is what stops one typo taking every satellite in the house
# deaf.
wake_words_available = ["alexa", "hey jarvis", "hey mycroft"]

[voice.elevenlabs]
# A third-party voice. OFF BY DEFAULT, and switching it on IS the consent
# (ADR-033 §2): it is the moment spoken output starts leaving the house.
#
# Requires [voice].wyoming_tts above — jarvisd refuses to start otherwise,
# because there would be nothing to fall back to. It can also be switched on and
# off from Settings -> Voice, which takes effect on the next sentence rather
# than the next restart; that toggle is refused under the same conditions.
#
# Message bodies and calendar entries are never routed here regardless of this
# setting (ADR-033 §4): sensitivity is a routing constraint, not a preference.
enabled = false
# api_key_ref = "keyring:jarvis/elevenlabs"
# voice_id = "…"
# model_id = "eleven_flash_v2_5"
# Characters per CALENDAR MONTH, counted durably in the database and rolled
# over on its own (ADR-033 §5). It was per-process until F8.8; that made
# "monthly" untrue in the direction that matters, since a daemon restarted daily
# had no ceiling at all.
#
# Exhaustion falls back to Piper rather than failing a turn — running out of a
# nicety must not cost an answer. The spend is visible in Settings -> Voice.
# character_budget = 100000

[timers]
enabled = true
# The daemon's own speaker, used when a timer has no room to ring in (F8.5).
# A timer set on a node rings on that node; this is the fallback.
alert_command = "paplay"
alert_args = ["-"]

# --- integrations (each off until named) ----------------------------------
# [integrations.home_assistant]
# base_url = "http://homeassistant.local:8123"
# token_secret = "keyring:jarvis/home-assistant"
# allowlist = ["light.kitchen", "scene.evening"]

# [integrations.smtp]
# enabled = false
# host = "smtp.example.com"
# from_address = "jarvis@example.com"
# password_secret = "keyring:jarvis/smtp"

[maps]
# Absent => map endpoints are not registered and the HUD shows its coverage
# fallback. The map never reaches a third-party tile service on its own.
# pmtiles_path = "/var/lib/jarvis/maps/region.pmtiles"

[ui]
background = "none"
panel_ttl_hours = 2
motion = "auto"

[observability]
# Off by default (docs/09 §5). Uncomment when a collector is running.
# otlp_endpoint = "http://127.0.0.1:4317"
