{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "AdapterHealth": {
      "properties": {
        "detail": {
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "$ref": "#/definitions/AdapterState"
        }
      },
      "required": [
        "state"
      ],
      "type": "object"
    },
    "AdapterState": {
      "description": "`disabled` = present in config but switched off (e.g. voice before M5).",
      "enum": [
        "up",
        "down",
        "disabled"
      ],
      "type": "string"
    },
    "AddListItemRequest": {
      "description": "`POST /api/v1/lists/{id}/items` — append one line.",
      "properties": {
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "AgendaEventDto": {
      "description": "The sensitivity-safe calendar subset shown on an agenda card (FR-35,\nADR-025). `start` and `end` are ISO-8601 display values supplied by the\ncalendar projection; the card never carries provider metadata or personal\ncontext beyond these four fields.",
      "properties": {
        "allDay": {
          "type": "boolean"
        },
        "end": {
          "type": "string"
        },
        "start": {
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "title",
        "start",
        "end",
        "allDay"
      ],
      "type": "object"
    },
    "AppLimitsDto": {
      "description": "Build limits the spec requests (docs/06 §6 \"size/time limits\"). Omitted\nfields mean the host ceiling; a value **above** the ceiling is rejected, not\nclamped.",
      "properties": {
        "maxBuildSeconds": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "maxBundleBytes": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AppSpecDto": {
      "description": "The app spec as it arrives from a model (FR-18 \"validated templates\").\nUntrusted in full: nothing here is authority, and everything here is\nvalidated by the domain before a build starts.",
      "properties": {
        "bindings": {
          "default": [],
          "items": {
            "$ref": "#/definitions/DataBindingDto"
          },
          "type": "array"
        },
        "capabilities": {
          "default": [],
          "description": "Capabilities the app declares it needs. May be empty — an app that needs\nno authority is the best kind. Unknown names are rejected.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "limits": {
          "anyOf": [
            {
              "$ref": "#/definitions/AppLimitsDto"
            },
            {
              "type": "null"
            }
          ]
        },
        "template": {
          "description": "Host template id, e.g. `dashboard/v1`. Unknown ids are rejected.",
          "type": "string"
        },
        "title": {
          "description": "Single-line title shown in the app's window chrome.",
          "type": "string"
        }
      },
      "required": [
        "template",
        "title"
      ],
      "type": "object"
    },
    "ApprovalCardDto": {
      "description": "What a human is asked to approve (docs/06 §3). Carries the exact effect and\nthe real proposed arguments so the approval binds precisely what is shown.",
      "properties": {
        "approvalId": {
          "$ref": "#/definitions/UlidString",
          "description": "The pending approval this card asks about; echoed back on the decision."
        },
        "egress": {
          "$ref": "#/definitions/DataEgressDto"
        },
        "exactEffect": {
          "description": "A human-readable rendering of exactly what will execute — the real target\nand payload, never a summary (docs/06 §3). Snapshot-tested.",
          "type": "string"
        },
        "proposedArguments": true,
        "reversible": {
          "description": "Whether the tool registered a compensating undo (docs/06 §4).",
          "type": "boolean"
        },
        "risk": {
          "$ref": "#/definitions/RiskLevelDto"
        },
        "runId": {
          "$ref": "#/definitions/UlidString"
        },
        "toolId": {
          "description": "The namespaced tool identifier (e.g. `message.send`).",
          "type": "string"
        }
      },
      "required": [
        "approvalId",
        "runId",
        "toolId",
        "exactEffect",
        "proposedArguments",
        "risk",
        "reversible",
        "egress"
      ],
      "type": "object"
    },
    "ApprovalDecision": {
      "description": "The verb a human answers a card with (docs/05 §4). Distinct from\n[`ApprovalResolutionDto`]'s past-tense outcome: this is the request input.",
      "enum": [
        "approve",
        "deny"
      ],
      "type": "string"
    },
    "ApprovalDecisionDto": {
      "description": "Body of `POST /api/v1/runs/{id}/approvals/{approval_id}` (docs/05 §4). On\n`approve`, `editedArguments` (when present) replaces the proposed arguments —\nthe grant binds the edited set, so executing the original would fail\nvalidation (invalidation by hash, not a flag; docs/06 §4). Ignored on `deny`.",
      "properties": {
        "decision": {
          "$ref": "#/definitions/ApprovalDecision"
        },
        "editedArguments": true
      },
      "required": [
        "decision"
      ],
      "type": "object"
    },
    "ApprovalResolutionDto": {
      "description": "How an approval was resolved (docs/05 §3), carried by the `approval.resolved`\nevent. Past tense to the [`ApprovalDecision`] request verb.",
      "enum": [
        "approved",
        "denied"
      ],
      "type": "string"
    },
    "ArtifactKindDto": {
      "description": "The logical kind of an artifact, selecting its renderer (docs/02 §6).",
      "enum": [
        "markdown_html",
        "code_text",
        "image",
        "chart",
        "bundle"
      ],
      "type": "string"
    },
    "ArtifactManifestDto": {
      "description": "One immutable artifact-version manifest (docs/02 §6, docs/04 §4, FR-08).",
      "properties": {
        "build": {
          "$ref": "#/definitions/BuildProvenanceDto"
        },
        "capabilities": {
          "description": "Declared capabilities, from the host's closed vocabulary (F6.1). A\nmanifest only ever holds validated capabilities, so the wire type is the\nexhaustive union rather than a string the client must interpret.",
          "items": {
            "$ref": "#/definitions/CapabilityDto"
          },
          "type": "array"
        },
        "createdByRun": {
          "$ref": "#/definitions/UlidString"
        },
        "id": {
          "$ref": "#/definitions/UlidString"
        },
        "kind": {
          "$ref": "#/definitions/ArtifactKindDto"
        },
        "mediaType": {
          "type": "string"
        },
        "renderer": {
          "description": "Versioned renderer id (docs/04 §4), derived from `kind`.",
          "type": "string"
        },
        "sensitivity": {
          "$ref": "#/definitions/ArtifactSensitivityDto"
        },
        "sha256": {
          "description": "Content address of the blob (lowercase hex sha256) — also the blob's\ndownload ETag.",
          "type": "string"
        },
        "sources": {
          "items": {
            "$ref": "#/definitions/ArtifactSourceDto"
          },
          "type": "array"
        },
        "version": {
          "description": "Monotonic version, 1-based.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "id",
        "version",
        "createdByRun",
        "sha256",
        "mediaType",
        "kind",
        "renderer",
        "sources",
        "sensitivity",
        "build",
        "capabilities"
      ],
      "type": "object"
    },
    "ArtifactSensitivityDto": {
      "description": "Sensitivity class of the artifact (NFR-02).",
      "enum": [
        "normal",
        "sensitive"
      ],
      "type": "string"
    },
    "ArtifactSourceDto": {
      "description": "One provenance source: what it is plus its reference (a ULID for\nmessage/run, a URL for web). The web shell renders these as a sources card\n(F3b.6), each with its own attribution (FR-27/ADR-017).",
      "properties": {
        "kind": {
          "$ref": "#/definitions/ArtifactSourceKindDto"
        },
        "reference": {
          "description": "The message/run ULID or the web URL, per `kind`.",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "reference"
      ],
      "type": "object"
    },
    "ArtifactSourceKindDto": {
      "description": "What kind of thing a provenance source refers to (docs/04 §4 `sources`).",
      "enum": [
        "message",
        "run",
        "web"
      ],
      "type": "string"
    },
    "ArtifactVersionsResponse": {
      "description": "`GET /api/v1/artifacts/{id}/versions` — every version of one artifact,\noldest first (docs/05 §1, FR-08).",
      "properties": {
        "artifactId": {
          "$ref": "#/definitions/UlidString"
        },
        "versions": {
          "items": {
            "$ref": "#/definitions/ArtifactManifestDto"
          },
          "type": "array"
        }
      },
      "required": [
        "artifactId",
        "versions"
      ],
      "type": "object"
    },
    "BuildNetworkDto": {
      "description": "The network policy under which the artifact was built (docs/04 §4).",
      "enum": [
        "disabled",
        "enabled"
      ],
      "type": "string"
    },
    "BuildProvenanceDto": {
      "description": "How the artifact was built (docs/04 §4 `build`). Carries only hashes and\nimage references — never secrets (invariant 5).",
      "properties": {
        "lockfileHash": {
          "type": [
            "string",
            "null"
          ]
        },
        "network": {
          "$ref": "#/definitions/BuildNetworkDto"
        },
        "workerImage": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "network"
      ],
      "type": "object"
    },
    "CanvasActionDto": {
      "description": "What this turn does to the materialization canvas (FR-24/FR-27, docs/12\n§2.5/§4) — the wire mirror of `jarvis_application::deepdive::CanvasAction`.\n\nExhaustive and deliberately two-valued: a third answer would change the\npanel lifecycle, which is a spec decision. Note what it cannot express —\nanything about approvals. Pending approval cards are exempt from shelving\n(docs/12 §4, F3b.4), and no value here can retract one.",
      "oneOf": [
        {
          "const": "extend",
          "description": "A continuation: append these cards, leave the prior ones in place.",
          "type": "string"
        },
        {
          "const": "shelve",
          "description": "A genuine topic change: shelve the current panels under `label` first.",
          "type": "string"
        }
      ]
    },
    "CapabilityDto": {
      "description": "A capability a validated artifact manifest declares (docs/04 §4\n`capabilities`). Exhaustive — the host vocabulary, mirrored on the wire.\nOne capability has **one** name on every surface: the dotted form the domain\nuses, the DB column stores, and an inbound `AppSpecDto.capabilities` string\nmust contain. `rename_all = \"snake_case\"` would have produced\n`home_read_state` here and `home.read_state` everywhere else, so a client\nreading a manifest could not put that string back into a spec (F6.1 review).",
      "oneOf": [
        {
          "const": "home.read_state",
          "description": "Read Home Assistant entity state (R0).",
          "type": "string"
        },
        {
          "const": "home.set_light",
          "description": "Set a single allowlisted light (R1).",
          "type": "string"
        },
        {
          "const": "home.execute_scene",
          "description": "Activate an allowlisted scene (R2).",
          "type": "string"
        }
      ]
    },
    "CapabilityResultDto": {
      "description": "What the operation produced. Deliberately narrow: a content string and\nwhether it was truncated. No grant, no policy decision, no audit id — an app\nlearns the answer to its question and nothing about the machinery that\ndecided to answer it.",
      "properties": {
        "content": {
          "type": "string"
        },
        "truncated": {
          "type": "boolean"
        }
      },
      "required": [
        "content",
        "truncated"
      ],
      "type": "object"
    },
    "CapabilityTokenDto": {
      "description": "The minted token. The value is a secret in the same sense a device token is:\nit is returned once, to one authenticated caller, and is spent by use.",
      "properties": {
        "capability": {
          "$ref": "#/definitions/CapabilityDto",
          "description": "Echoed so a client cannot mix up which token is for which capability."
        },
        "expiresAt": {
          "description": "RFC 3339 instant after which the token is dead.",
          "type": "string"
        },
        "token": {
          "description": "Opaque 32-byte hex.",
          "type": "string"
        }
      },
      "required": [
        "token",
        "expiresAt",
        "capability"
      ],
      "type": "object"
    },
    "Channel": {
      "description": "One WebSocket replaces v1's three hubs; `channel` discriminates (docs/05 §1).",
      "enum": [
        "session",
        "display",
        "voice"
      ],
      "type": "string"
    },
    "CheckListItemRequest": {
      "description": "`PATCH /api/v1/lists/{id}/items/{itemId}` — the card's check-off tap, and\nfrom M5 the voice path. Deliberately a whole-value set rather than a toggle:\ntwo devices tapping the same line converge on the same result instead of\nracing each other back and forth.",
      "properties": {
        "checked": {
          "type": "boolean"
        }
      },
      "required": [
        "checked"
      ],
      "type": "object"
    },
    "ContentBlock": {
      "oneOf": [
        {
          "properties": {
            "text": {
              "type": "string"
            },
            "type": {
              "const": "text",
              "type": "string"
            }
          },
          "required": [
            "type",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "Forward-compatibility catch-all (docs/05 §5). Never produced by this\nversion's writers; only ever the result of reading a newer peer.",
          "properties": {
            "type": {
              "const": "unknown",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "CreateListRequest": {
      "description": "`POST /api/v1/lists` — create a named list, or return the existing one with\nthe same normalized name (creation is idempotent on the name key, because\n\"add milk to the shopping list\" must work before a shopping list exists).",
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "CreateSessionRequest": {
      "properties": {
        "title": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CreateTimerRequest": {
      "description": "`POST /api/v1/timers`. Exactly one of `durationSecs` (a countdown) or\n`fireAt` (an alarm/reminder) must be given; `note` is required by — and only\nmeaningful for — `reminder`.",
      "properties": {
        "durationSecs": {
          "description": "\"…in ten minutes\", relative to the server's clock at set time.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "fireAt": {
          "description": "\"…at seven\", RFC 3339. Absolute so the server never has to guess a\ntimezone or a \"next Tuesday\".",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/definitions/TimerKindDto"
        },
        "name": {
          "description": "Optional label. Omitted ⇒ the server uses the kind's default (\"Timer\",\n\"Alarm\", \"Reminder\") so every timer is enumerable and addressable.",
          "type": [
            "string",
            "null"
          ]
        },
        "note": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "DataBindingDto": {
      "description": "One data binding the app declares (see [`AppSpecDto`]).",
      "properties": {
        "capability": {
          "description": "The declared capability that supplies it — validated host-side against\nthe closed vocabulary.",
          "type": "string"
        },
        "name": {
          "description": "Identifier the template binds this data under (`[a-z][a-z0-9_]*`).",
          "type": "string"
        },
        "target": {
          "description": "The resource it addresses (e.g. an entity id). Opaque to the host; the\nbacking tool's own allowlist resolves it at call time.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "capability",
        "target"
      ],
      "type": "object"
    },
    "DataEgressDto": {
      "description": "Wire projection of [`jarvis_domain::policy::DataEgress`] (docs/06 §5): how far\nthe tool's data may travel, surfaced so the human approves egress knowingly.",
      "enum": [
        "none",
        "local",
        "external"
      ],
      "type": "string"
    },
    "DeepDiveFindingsRequest": {
      "description": "`POST /api/v1/sessions/{id}/deepdive/findings` — what this turn learned.\n\n`facts` are **paraphrases Jarvis composed**, not fetched page text: the\ndomain refuses anything over\n`jarvis_domain::deepdive::MAX_PARAPHRASE_CHARS` rather than truncating it,\nbecause a truncated scrape is still a scrape (ADR-017). Everything in this\nrequest is filed through the thread's guarded recorders; a rejected entry is\nreported in [`DeepDiveFindingsResponse::refused`] and simply does not exist\nin the thread.\n\nEach array carries **at most 64 entries** — a whole request over that is\nrefused with `422 validation.failed` rather than partially filed, because the\nloop that consumes it holds a process-global lock and its length is how long\nevery other conversation's turn waits. A turn files what it just consulted;\n64 of each is far past anything real and well under the thread's own totals.",
      "properties": {
        "facts": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "images": {
          "default": [],
          "items": {
            "$ref": "#/definitions/ImageFindingDto"
          },
          "type": "array"
        },
        "sources": {
          "default": [],
          "items": {
            "$ref": "#/definitions/SourceFindingDto"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "DeepDiveFindingsResponse": {
      "description": "What was actually filed. Counts, plus a plain-text reason per rejected\nentry so a caller learns *that* a scrape or an unattributable URL was\nrefused rather than discovering it missing later.\n\nThe reasons are a **fixed vocabulary the server owns**: they never quote the\noffending fact, title or URL back at the caller. Reflecting an\narbitrary-length, unsanitized, caller-chosen string into a response body is\nthe mistake `ListError` documents avoiding, and a caller already knows what\nit sent.",
      "properties": {
        "facts": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "images": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "refused": {
          "description": "One line per refused entry. Empty when everything was filed.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sources": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "facts",
        "sources",
        "images"
      ],
      "type": "object"
    },
    "DisplayDirective": {
      "description": "A directive the server sends to the agent on the `display` channel. The\n`type` discriminator is dotted-namespaced (`display.place_surface`), matching\nthe envelope convention; the agent routes on it.",
      "oneOf": [
        {
          "description": "Move the window for `surface` (identified by its stable Chromium app-mode\n`appId`) onto `monitor` (a compositor connector name, e.g. `DP-1`). The\n`appId` always comes from the closed server-side surface set — never from\nmodel or user text — and the agent additionally refuses any `appId`\noutside the `jarvis.` namespace (defense in depth).",
          "properties": {
            "appId": {
              "type": "string"
            },
            "monitor": {
              "type": "string"
            },
            "surface": {
              "$ref": "#/definitions/SurfaceDto"
            },
            "type": {
              "const": "display.place_surface",
              "type": "string"
            }
          },
          "required": [
            "type",
            "surface",
            "appId",
            "monitor"
          ],
          "type": "object"
        },
        {
          "description": "Open `url` in the **dedicated media window** on `monitor` (FR-22, ADR-012\ncast-a-link): launch it if it is not running, reuse it if it is, then\nplace it. The window has its own app-id (`jarvis.media`), its own profile\ndirectory, and **no credentials** — it renders third-party web video and\nis deliberately isolated from both the shell and the browser worker's\nprofiles (docs/02 §11a).\n\nThis is the one directive that causes the agent to **launch a process**,\nso the constraints are part of the contract, not an implementation\ndetail: `url` must be `https`, and the agent independently re-validates\nit and launches only a fixed, allowlisted browser command with the URL as\na single argv element — never through a shell (docs/02 §8: \"it is not a\nshell\").",
          "properties": {
            "monitor": {
              "type": "string"
            },
            "type": {
              "const": "display.open_media_url",
              "type": "string"
            },
            "url": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "url",
            "monitor"
          ],
          "type": "object"
        }
      ]
    },
    "DomainEvent": {
      "description": "Persisted, replayable events (docs/05 §3 \"persisted event categories\").\nEvery variant must be representable in the timeline snapshot — a client that\nmissed it while disconnected recovers it via `GET /sessions/{id}/timeline`.\n\nThe `type` discriminator is **dotted-namespaced** (`run.started`,\n`message.created`), matching the envelope example in docs/05 §3\n(`run.tool.completed`) and the error-code scheme — clients route on this\nstring, so it is load-bearing and every tag is spelled explicitly below.\n\nThis union is intentionally **strict**: there is no `Unknown` catch-all\n(unlike [`crate::content::ContentBlock`], which is open-world because blocks\noriginate from external providers). Every `DomainEvent` is authored by\njarvisd itself, and the web shell is served by the same binary — so producer\nand consumer share one contract version and can never skew. A tag the reader\ndoes not recognize is therefore a genuine bug we want surfaced as a decode\nerror, not silently dropped from a resync page. New variants are added\nadditively within a version (docs/05 §3).",
      "oneOf": [
        {
          "properties": {
            "runId": {
              "$ref": "#/definitions/UlidString"
            },
            "sessionId": {
              "$ref": "#/definitions/UlidString"
            },
            "type": {
              "const": "run.started",
              "type": "string"
            }
          },
          "required": [
            "type",
            "runId",
            "sessionId"
          ],
          "type": "object"
        },
        {
          "properties": {
            "runId": {
              "$ref": "#/definitions/UlidString"
            },
            "state": {
              "$ref": "#/definitions/RunStateDto"
            },
            "type": {
              "const": "run.state_changed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "runId",
            "state"
          ],
          "type": "object"
        },
        {
          "description": "Degraded mode: the run is parked awaiting provider recovery (FR-12) —\na visible waiting state, replayed so a reconnecting client still sees it.",
          "properties": {
            "reason": {
              "type": "string"
            },
            "runId": {
              "$ref": "#/definitions/UlidString"
            },
            "type": {
              "const": "run.queued",
              "type": "string"
            }
          },
          "required": [
            "type",
            "runId",
            "reason"
          ],
          "type": "object"
        },
        {
          "properties": {
            "outcome": {
              "$ref": "#/definitions/RunOutcome"
            },
            "runId": {
              "$ref": "#/definitions/UlidString"
            },
            "type": {
              "const": "run.completed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "runId",
            "outcome"
          ],
          "type": "object"
        },
        {
          "properties": {
            "message": {
              "$ref": "#/definitions/MessageDto"
            },
            "type": {
              "const": "message.created",
              "type": "string"
            }
          },
          "required": [
            "type",
            "message"
          ],
          "type": "object"
        },
        {
          "properties": {
            "provider": {
              "$ref": "#/definitions/ProviderDto"
            },
            "type": {
              "const": "provider.health_changed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "provider"
          ],
          "type": "object"
        },
        {
          "description": "Recovery checkpoint (NFR-05/13); replayed so resync reflects the last\nsafe boundary a restart would resume from.",
          "properties": {
            "runId": {
              "$ref": "#/definitions/UlidString"
            },
            "state": {
              "$ref": "#/definitions/RunStateDto"
            },
            "type": {
              "const": "run.checkpoint_saved",
              "type": "string"
            }
          },
          "required": [
            "type",
            "runId",
            "state"
          ],
          "type": "object"
        },
        {
          "description": "An R2/R3 tool proposal is parked awaiting a human decision (F2.5,\ndocs/06 §3). Replayed so a client reconnecting while a run sits at\n`WaitingApproval` still sees the pending card and can act on it.",
          "properties": {
            "card": {
              "$ref": "#/definitions/ApprovalCardDto"
            },
            "type": {
              "const": "approval.requested",
              "type": "string"
            }
          },
          "required": [
            "type",
            "card"
          ],
          "type": "object"
        },
        {
          "description": "A timer, alarm or reminder went off (F3b.7, FR-33, ADR-023).\n\nPersisted rather than transient, deliberately: a timer going off is a\n*fact about a moment*, and a client that was disconnected when the\nkitchen timer rang must still learn about it on resync — unlike\n`media.state`, which is a current-value readout with no history worth\nreplaying. It is also not run-scoped (a timer fires with no run in\nflight), which is why it carries no `RunId`.\n\nThis feature is the event's only producer: the timer module fires it, and\nnothing else may. `missed` is the honest notice ADR-023 requires — the\ntimer came due while jarvisd was not running, and the human is told so\nrather than shown something that looks like it just rang.",
          "properties": {
            "missed": {
              "type": "boolean"
            },
            "timer": {
              "$ref": "#/definitions/TimerDto"
            },
            "type": {
              "const": "timer.fired",
              "type": "string"
            }
          },
          "required": [
            "type",
            "timer",
            "missed"
          ],
          "type": "object"
        },
        {
          "description": "A pending approval was answered (F2.5). Replayed so the resolved outcome\nsurvives reconnect and the client can retire the card it was showing.",
          "properties": {
            "approvalId": {
              "$ref": "#/definitions/UlidString"
            },
            "outcome": {
              "$ref": "#/definitions/ApprovalResolutionDto"
            },
            "runId": {
              "$ref": "#/definitions/UlidString"
            },
            "type": {
              "const": "approval.resolved",
              "type": "string"
            }
          },
          "required": [
            "type",
            "approvalId",
            "runId",
            "outcome"
          ],
          "type": "object"
        }
      ]
    },
    "ErrorCode": {
      "oneOf": [
        {
          "enum": [
            "auth.invalid_token",
            "auth.scope_missing",
            "auth.pairing_invalid",
            "validation.failed",
            "idempotency.conflict",
            "resource.version_conflict",
            "resource.not_found",
            "run.budget_exceeded",
            "run.not_cancellable",
            "provider.unavailable",
            "provider.quota_exhausted",
            "policy.denied",
            "grant.expired",
            "grant.args_mismatch",
            "grant.consumed",
            "tool.timeout",
            "tool.result_invalid",
            "artifact.too_large",
            "degraded.queued",
            "memory.secret_rejected",
            "memory.invalid"
          ],
          "type": "string"
        },
        {
          "const": "artifact.integrity_failed",
          "description": "A stored artifact blob failed content-address verification on read —\non-disk corruption or tampering (F3a.2 CAS verify-on-read). 500; the\nbytes are never returned (fail closed).",
          "type": "string"
        },
        {
          "const": "media.nothing_playing",
          "description": "No media player is running, so an untargeted transport command has\nnothing to act on (F3a.7, FR-22). 409. Distinct from\n`media.target_ambiguous`: the client shows an empty state rather than\nasking which player.",
          "type": "string"
        },
        {
          "const": "media.target_ambiguous",
          "description": "Two or more players are active and the request named none, so the server\nrefuses to guess (ADR-016 — the choice is asked, never inferred). 409.\nThe client's cue to ask which player and retry with `player` set.",
          "type": "string"
        },
        {
          "const": "media.player_gone",
          "description": "The named player left the bus between the snapshot and the command. 409.\nA normal race, and safely retryable after a state refresh — which is why\nit is not `resource.not_found` (that means \"no such id, ever\").",
          "type": "string"
        },
        {
          "const": "media.control_unsupported",
          "description": "The player is present but reports it cannot perform this control\n(`CanGoNext = false`). 409 — the request was well-formed, so this is not\n`validation.failed`; retrying the same call will not help, but a\ndifferent control on the same player may.",
          "type": "string"
        },
        {
          "const": "timer.invalid_transition",
          "description": "The requested verb is not legal for the timer's current state — snoozing\none that has not rung, cancelling one that already did (F3b.7, ADR-023).\n409: the request was well-formed, so it is not `validation.failed`, and\nretrying it unchanged will not help; the client re-reads the timer first.",
          "type": "string"
        },
        {
          "const": "timer.stale",
          "description": "The timer moved between the client's read and its decision — it fired in\nthe same instant, or another device answered first. 409 and **safely\nretryable** after a refresh, which is why it is distinct from\n`timer.invalid_transition`.",
          "type": "string"
        },
        {
          "const": "list.full",
          "description": "The list already holds the maximum number of items (F3b.8, ADR-024).\n409: the request was well-formed, so it is not `validation.failed`, and\nretrying it unchanged will not help — the owner removes or checks off\nsomething first, or promotes the list to an artifact, which is exactly\nwhat promotion is for.",
          "type": "string"
        },
        {
          "const": "list.unrecognized_command",
          "description": "The utterance is not unambiguously a list command, so the deterministic\ngrammar refused rather than guessing which list the owner meant\n(F3b.8, ADR-024/ADR-016). 422: the body was valid, the *content* was not\nresolvable here — the caller falls back to the normal run path.",
          "type": "string"
        },
        {
          "const": "deepdive.nothing_to_promote",
          "description": "The deep-dive thread has accumulated nothing worth keeping, so there is\nno Research Notes document to write (F3b.6, FR-27/ADR-017). 409: the\nrequest was well-formed and retrying it unchanged will not help — the\nthread has to consult something first. Promoting a bare heading would\nmint a versioned artifact that says nothing.",
          "type": "string"
        },
        {
          "const": "app.undeclared_capability",
          "description": "A generated app asked for a capability its own manifest does not declare\n(F6.5, docs/06 §6). 403 — the request was authenticated and well-formed;\nthe app simply has no such authority to ask for. The audit row\n`app.capability_denied` is the durable half of this rejection.",
          "type": "string"
        },
        {
          "const": "app.token_rejected",
          "description": "A capability token was unknown, spent, expired, or bound to a different\napp, version, capability or device (F6.5). 403, one code for all of them:\ntelling a caller *which* would let it probe the token space.",
          "type": "string"
        },
        {
          "const": "app.invalid_request",
          "description": "The bridge request named a target the domain refuses to carry (F6.5).\n400.",
          "type": "string"
        }
      ]
    },
    "EventEnvelope": {
      "properties": {
        "channel": {
          "$ref": "#/definitions/Channel"
        },
        "occurredAt": {
          "description": "RFC 3339 timestamp of the domain occurrence.",
          "type": "string"
        },
        "payload": true,
        "resourceVersion": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "seq": {
          "description": "Monotonic per connection scope; gaps trigger client resync (NFR-13).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "traceId": {
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "description": "Event type discriminator, e.g. `run.tool.completed`.",
          "type": "string"
        },
        "v": {
          "description": "Contract version ([`crate::CONTRACT_VERSION`]).",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "v",
        "seq",
        "channel",
        "type",
        "occurredAt",
        "payload"
      ],
      "type": "object"
    },
    "HeadlineItemDto": {
      "description": "One item of a headlines/digest card (docs/12 §2.3: \"3-5 short items, each a\none-line title + one-line summary + relative time + source link\"). The item\ncarries its own source link independent of `thumbnail`'s attribution,\nbecause a digest's items may each come from a different page.",
      "properties": {
        "relativeTime": {
          "description": "Pre-formatted relative time (e.g. \"2h ago\"), computed once server-side\n— the client renders it as-is rather than ticking a live clock against\na fact that is not actually live.",
          "type": "string"
        },
        "sourceDomain": {
          "type": "string"
        },
        "sourceUrl": {
          "type": "string"
        },
        "summary": {
          "type": "string"
        },
        "thumbnail": {
          "anyOf": [
            {
              "$ref": "#/definitions/SourcedImageDto"
            },
            {
              "type": "null"
            }
          ],
          "description": "Thumbnail is optional (docs/12 §2.3: \"no photos required, thumbnail\noptional\"); when present it still carries its own attribution."
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "title",
        "summary",
        "relativeTime",
        "sourceUrl",
        "sourceDomain"
      ],
      "type": "object"
    },
    "HealthResponse": {
      "properties": {
        "adapters": {
          "additionalProperties": {
            "$ref": "#/definitions/AdapterHealth"
          },
          "description": "Adapter readiness by adapter name (docs/02 §12 startup order).",
          "type": "object"
        },
        "pairingCode": {
          "description": "One-time pairing code, present only while the first-run pairing\nwindow is open (docs/05 §6: shown on the health page, loopback only).",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/definitions/ServiceStatus"
        },
        "ui": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiSettingsDto"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional for embedders and older test fixtures during additive rollout."
        },
        "version": {
          "description": "jarvisd semver, for support/diagnostics.",
          "type": "string"
        }
      },
      "required": [
        "status",
        "version",
        "adapters"
      ],
      "type": "object"
    },
    "HudCanvasDto": {
      "description": "One canvas instruction (F3b.6). The payload of the transient `hud.canvas`\nevent.\n\n`cards` is the **live card set for this canvas**, not a delta: the same card\nid re-sent is the same card refreshed, so a client that applies it\nupsert-by-id converges on the current state no matter how many events it\nmissed. That is what makes a transient classification safe here.",
      "properties": {
        "action": {
          "$ref": "#/definitions/CanvasActionDto"
        },
        "cards": {
          "items": {
            "$ref": "#/definitions/HudCardDto"
          },
          "type": "array"
        },
        "handoff": {
          "anyOf": [
            {
              "$ref": "#/definitions/SourceHandoffDto"
            },
            {
              "type": "null"
            }
          ],
          "description": "Present when the human asked to read one of the cited sources."
        },
        "label": {
          "description": "The shelf chip label to file the *displaced* panels under when `action`\nis `shelve` (docs/12 §4: \"Ramen places · Restore · ×\"). Plain text.",
          "type": "string"
        },
        "offer": {
          "description": "The spoken offer to keep this thread as a Research Notes document\n(docs/12 §2.5: Jarvis's normal voice, one line, never a dialog box).\nPresent only on the turn that crosses `[ui] deepdive_promote_after`.",
          "type": [
            "string",
            "null"
          ]
        },
        "sessionId": {
          "anyOf": [
            {
              "$ref": "#/definitions/UlidString"
            },
            {
              "type": "null"
            }
          ],
          "description": "The conversation this turn belongs to. Absent for a canvas update that\nis not part of a deep-dive thread at all — a list card produced by the\ndeterministic list grammar (FR-34), which has no session."
        }
      },
      "required": [
        "action",
        "label"
      ],
      "type": "object"
    },
    "HudCardDto": {
      "description": "Registered HUD card types (docs/12 §2.3). The `type` discriminator is\ndotted-namespaced (`card.value_readout`, …), matching the envelope/event\nconvention. **Strict, no catch-all** — every card is authored by jarvisd\nitself (there is no third-party card producer), the same reasoning as\n[`crate::events::DomainEvent`]. The client-side defense for a genuinely\nunrecognized discriminant (a future contract version, a malformed payload)\nis the Angular `hud-card` switch component degrading to the error card —\nbelt-and-suspenders, not a substitute for this union staying the single\nsource of truth for what \"registered\" means.",
      "oneOf": [
        {
          "description": "Hero number with optional staggered mini-stats (docs/12 §2.3): weather,\na single metric, a quick count.",
          "properties": {
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            },
            "miniStats": {
              "items": {
                "$ref": "#/definitions/MiniStatDto"
              },
              "type": "array"
            },
            "type": {
              "const": "card.value_readout",
              "type": "string"
            },
            "value": {
              "description": "The hero value as display text (e.g. \"72°F\") — the client applies\ntabular-nums and the count-up animation, it does not compute the\nvalue.",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "label",
            "value"
          ],
          "type": "object"
        },
        {
          "description": "A place result (docs/12 §2.3): photo, rating/distance/price pills, and\nthe `pick` variant (hue ring) marking a top recommendation.",
          "properties": {
            "distance": {
              "type": [
                "string",
                "null"
              ]
            },
            "id": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "photo": {
              "anyOf": [
                {
                  "$ref": "#/definitions/SourcedImageDto"
                },
                {
                  "type": "null"
                }
              ]
            },
            "pick": {
              "default": false,
              "description": "Marks the top recommendation among a set of place cards — rendered\nwith a hue ring (docs/12 §2.3), never a colour picked ad hoc.",
              "type": "boolean"
            },
            "priceLevel": {
              "type": [
                "string",
                "null"
              ]
            },
            "rating": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "card.place",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "name"
          ],
          "type": "object"
        },
        {
          "description": "An entity/person result (docs/12 §2.3): photo, confidence, facts.",
          "properties": {
            "confidencePct": {
              "description": "0-100 confidence in the entity resolution.",
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "facts": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "id": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "photo": {
              "anyOf": [
                {
                  "$ref": "#/definitions/SourcedImageDto"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "const": "card.entity",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "name"
          ],
          "type": "object"
        },
        {
          "description": "Media/menu grid (docs/12 §2.3: \"photo + name + price\").",
          "properties": {
            "id": {
              "type": "string"
            },
            "items": {
              "items": {
                "$ref": "#/definitions/MediaGridItemDto"
              },
              "type": "array"
            },
            "title": {
              "type": "string"
            },
            "type": {
              "const": "card.media_grid",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "title",
            "items"
          ],
          "type": "object"
        },
        {
          "description": "Headlines/digest (docs/12 §2.3: several current items, not one fact —\ndistinct from a single entity/value-readout card for that reason).",
          "properties": {
            "id": {
              "type": "string"
            },
            "items": {
              "items": {
                "$ref": "#/definitions/HeadlineItemDto"
              },
              "type": "array"
            },
            "title": {
              "type": "string"
            },
            "type": {
              "const": "card.headlines",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "title",
            "items"
          ],
          "type": "object"
        },
        {
          "description": "A bounded read-only agenda (FR-35, ADR-025). The producer supplies at\nmost one local-day window of events; no mutation affordance or calendar\nprovider details are represented on the HUD card.",
          "properties": {
            "events": {
              "items": {
                "$ref": "#/definitions/AgendaEventDto"
              },
              "maxItems": 256,
              "type": "array"
            },
            "id": {
              "type": "string"
            },
            "title": {
              "type": "string"
            },
            "type": {
              "const": "card.agenda",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "title",
            "events"
          ],
          "type": "object"
        },
        {
          "description": "\"What's playing\" as a first-class query (docs/12 §2.3, FR-32/ADR-022):\n**data only** — this variant answers a query, it does not add playback\ncontrols, and M5/F5.7 produced it without adding any. Live control stays\non the media bar (`crate::media`) and, for the model, on the registered\n`media.playback` tool behind `policy::evaluate`; a control on this card\nwould be an effect reachable from a text match. Album art here is the\nplayer's own content, not third-party web content, so it is a plain\n(already `https`-validated) URL rather than a [`SourcedImageDto`] — no\nsource chip is owed for a player showing its own art, matching the\nmedia bar's existing treatment.",
          "properties": {
            "album": {
              "type": [
                "string",
                "null"
              ]
            },
            "artUrl": {
              "type": [
                "string",
                "null"
              ]
            },
            "artist": {
              "type": [
                "string",
                "null"
              ]
            },
            "id": {
              "type": "string"
            },
            "sourceApp": {
              "type": "string"
            },
            "title": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "card.now_playing",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "sourceApp"
          ],
          "type": "object"
        },
        {
          "description": "A map result (F3b.5, docs/12 §3, ADR-013, FR-25): destination pin,\noptional current-location dot, optional route polyline, and\npre-formatted distance/walk-time text. See the module doc comment for\nwhy there is no rendering-mode field — local-vs-raster-vs-coordinates\nis a client-side decision made from `crate::maps::MapCoverageResponse`\nat render time, not something baked in here.",
          "properties": {
            "currentLocation": {
              "anyOf": [
                {
                  "$ref": "#/definitions/MapPointDto"
                },
                {
                  "type": "null"
                }
              ]
            },
            "destination": {
              "$ref": "#/definitions/MapPointDto"
            },
            "destinationLabel": {
              "type": [
                "string",
                "null"
              ]
            },
            "distance": {
              "description": "Pre-formatted distance display text (e.g. \"1.2 mi\"), same\nconvention as every other card's display-text fields — the client\napplies tabular-nums, it does not compute the value.",
              "type": [
                "string",
                "null"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            },
            "route": {
              "description": "Route polyline vertices, in order. Empty when there is no route to\ndraw (a bare \"where is X\" query with no navigation intent).",
              "items": {
                "$ref": "#/definitions/MapPointDto"
              },
              "type": "array"
            },
            "type": {
              "const": "card.map",
              "type": "string"
            },
            "walkTime": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "type",
            "id",
            "label",
            "destination"
          ],
          "type": "object"
        },
        {
          "description": "The pages a deep dive consulted (docs/12 §2.3/§2.5, FR-27/ADR-017) —\nwhat \"show me the references\" materializes. A bibliography, not a\nreader: each item is a title, a domain chip and a link, and opening one\nis a browser handoff (ADR-017 §3), never page content rendered here.",
          "properties": {
            "id": {
              "type": "string"
            },
            "items": {
              "items": {
                "$ref": "#/definitions/SourceItemDto"
              },
              "type": "array"
            },
            "title": {
              "type": "string"
            },
            "type": {
              "const": "card.sources",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "title",
            "items"
          ],
          "type": "object"
        },
        {
          "description": "A small image grid (docs/12 §2.3, FR-27/ADR-017), capped at\n`jarvis_domain::deepdive::GALLERY_IMAGE_CAP` by the producer.\n\n**Per-tile attribution is structural**: the images are full\n[`SourcedImageDto`]s and this variant has no card-level source field, so\nthere is no way to express \"one source for all of these\" — which ADR-017\nforbids, because a gallery's images routinely come from different pages.",
          "properties": {
            "id": {
              "type": "string"
            },
            "images": {
              "items": {
                "$ref": "#/definitions/SourcedImageDto"
              },
              "type": "array"
            },
            "title": {
              "type": "string"
            },
            "type": {
              "const": "card.gallery",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "title",
            "images"
          ],
          "type": "object"
        },
        {
          "description": "A named list with its items (docs/12 §2.3, FR-34/ADR-024): the face of\n\"what's on the shopping list\", with check-off by voice or tap. The list\nitself is wire-reused from [`crate::lists::ListDto`] rather than\nre-modeled, the same discipline as the approval variant below — there is\nexactly one type that says what a list is on the wire.\n\n`listId` rides alongside the card's own `id` because the check-off tap\nposts to `/api/v1/lists/{listId}/items/{itemId}`: a card id is a\npresentation handle, never an address the client may derive a write from.",
          "properties": {
            "id": {
              "type": "string"
            },
            "list": {
              "$ref": "#/definitions/ListDto"
            },
            "listId": {
              "$ref": "#/definitions/UlidString"
            },
            "type": {
              "const": "card.list",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "listId",
            "list"
          ],
          "type": "object"
        },
        {
          "description": "The approval surface, wire-reused verbatim (docs/06 §3) — never\nre-modeled as a distinct card shape, so there is exactly one type that\ncarries `exactEffect`/`proposedArguments` on the wire.",
          "properties": {
            "card": {
              "$ref": "#/definitions/ApprovalCardDto"
            },
            "type": {
              "const": "card.approval",
              "type": "string"
            }
          },
          "required": [
            "type",
            "card"
          ],
          "type": "object"
        },
        {
          "description": "A transient status readout (docs/12 §2.3), e.g. a run parked in\ndegraded-mode queueing (FR-12).",
          "properties": {
            "id": {
              "type": "string"
            },
            "message": {
              "type": "string"
            },
            "queued": {
              "default": false,
              "type": "boolean"
            },
            "type": {
              "const": "card.status",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "message"
          ],
          "type": "object"
        },
        {
          "description": "The fallback face for a failure — and the client's own degrade target\nfor an unrecognized discriminant (docs/12 §9).",
          "properties": {
            "id": {
              "type": "string"
            },
            "message": {
              "type": "string"
            },
            "type": {
              "const": "card.error",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "message"
          ],
          "type": "object"
        }
      ]
    },
    "ImageFindingDto": {
      "description": "One image a turn referenced, with **its own** page (ADR-017: provenance\ndiffers per image, so one shared attribution is not acceptable).",
      "properties": {
        "alt": {
          "type": "string"
        },
        "sourceUrl": {
          "description": "The page this image was found on — never a neighbour's.",
          "type": "string"
        },
        "url": {
          "type": "string"
        }
      },
      "required": [
        "alt",
        "url",
        "sourceUrl"
      ],
      "type": "object"
    },
    "InvokeCapabilityRequest": {
      "additionalProperties": false,
      "description": "`POST /api/v1/apps/{id}/versions/{version}/invoke` — exchange a token for one\noperation.",
      "properties": {
        "capability": {
          "$ref": "#/definitions/CapabilityDto"
        },
        "target": {
          "description": "The resource the operation applies to. Validated by the domain and then\nre-resolved by the backing tool's own allowlist — naming it confers\nnothing (ADR-029).",
          "type": "string"
        },
        "token": {
          "type": "string"
        },
        "value": {
          "description": "The value the operation takes, where it takes one.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "capability",
        "target",
        "token"
      ],
      "type": "object"
    },
    "ListCommandRequest": {
      "description": "`POST /api/v1/lists/command` — run the **deterministic grammar** over one\nutterance (ADR-024). Zero model calls: an utterance the grammar does not\nunambiguously recognize is a `list.unrecognized_command` 422, never a guess.\n422 rather than 400 because the body was perfectly valid — it is the\n*content* that did not resolve here, and the caller's answer is to fall back\nto the normal run path rather than to fix its request.",
      "properties": {
        "utterance": {
          "description": "The utterance as spoken or typed, e.g. \"add milk to the shopping list\".",
          "type": "string"
        }
      },
      "required": [
        "utterance"
      ],
      "type": "object"
    },
    "ListCommandResponse": {
      "description": "The list after the command was applied and audited, so the card re-renders\nwithout waiting for an event.",
      "properties": {
        "effect": {
          "$ref": "#/definitions/ListEffectDto"
        },
        "itemId": {
          "anyOf": [
            {
              "$ref": "#/definitions/UlidString"
            },
            {
              "type": "null"
            }
          ],
          "description": "The item the command touched, absent for a read."
        },
        "list": {
          "$ref": "#/definitions/ListDto"
        }
      },
      "required": [
        "list",
        "effect"
      ],
      "type": "object"
    },
    "ListDto": {
      "description": "One named list with its items, in insertion order.",
      "properties": {
        "id": {
          "$ref": "#/definitions/UlidString"
        },
        "items": {
          "items": {
            "$ref": "#/definitions/ListItemDto"
          },
          "type": "array"
        },
        "name": {
          "description": "Display name as the owner gave it (\"Shopping\"), sanitized.",
          "type": "string"
        },
        "openCount": {
          "description": "How many items are still open — the readback's \"two things left\"\ncomputed once server-side so the card and the spoken answer agree.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "promotedArtifactId": {
          "anyOf": [
            {
              "$ref": "#/definitions/UlidString"
            },
            {
              "type": "null"
            }
          ],
          "description": "The versioned artifact this list has been promoted into, if any (FR-08).\nPresent ⇒ a further promotion adds a *version* to this artifact."
        },
        "promotionOffered": {
          "description": "The list has grown into a document and promotion is worth offering\n(ADR-024). An offer the shell surfaces, never an automatic conversion.",
          "type": "boolean"
        }
      },
      "required": [
        "id",
        "name",
        "items",
        "openCount",
        "promotionOffered"
      ],
      "type": "object"
    },
    "ListEffectDto": {
      "description": "What a grammar command did.",
      "oneOf": [
        {
          "enum": [
            "added",
            "removed",
            "checked_off"
          ],
          "type": "string"
        },
        {
          "const": "read",
          "description": "A pure query — nothing was written and nothing was audited.",
          "type": "string"
        }
      ]
    },
    "ListIndexResponse": {
      "description": "`GET /api/v1/lists` — every list the owner has, name-ordered.",
      "properties": {
        "lists": {
          "items": {
            "$ref": "#/definitions/ListDto"
          },
          "type": "array"
        }
      },
      "required": [
        "lists"
      ],
      "type": "object"
    },
    "ListItemDto": {
      "description": "One line on a list.",
      "properties": {
        "checked": {
          "type": "boolean"
        },
        "id": {
          "$ref": "#/definitions/UlidString"
        },
        "text": {
          "description": "Sanitized human text. Rendered as text.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "text",
        "checked"
      ],
      "type": "object"
    },
    "MapBoundsDto": {
      "description": "A WGS84 bounding box in degrees, `min <= max` on both axes. As an archive's\ncoverage, it is the region the extract covers: outside it the client must\nfall back (docs/12 §3), and the server refuses those tiles either way.",
      "properties": {
        "maxLat": {
          "format": "double",
          "type": "number"
        },
        "maxLon": {
          "format": "double",
          "type": "number"
        },
        "minLat": {
          "format": "double",
          "type": "number"
        },
        "minLon": {
          "format": "double",
          "type": "number"
        }
      },
      "required": [
        "minLon",
        "minLat",
        "maxLon",
        "maxLat"
      ],
      "type": "object"
    },
    "MapCenterDto": {
      "description": "The archive's suggested opening view.",
      "properties": {
        "lat": {
          "format": "double",
          "type": "number"
        },
        "lon": {
          "format": "double",
          "type": "number"
        },
        "zoom": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "lon",
        "lat",
        "zoom"
      ],
      "type": "object"
    },
    "MapCoverageResponse": {
      "description": "`GET /api/v1/map/coverage` — what the locally served archive covers.\n\nThe endpoint exists only when an archive is configured (`[maps]\npmtiles_path`); with none, the map routes are not registered at all and this\nis a 404, which the client reads as \"no local map, use the fallback\" —\nabsent rather than broken.",
      "properties": {
        "attribution": {
          "description": "Mandatory, non-empty, plain text, and always names OpenStreetMap\n(docs/12 §3: attribution is never hidden).",
          "type": "string"
        },
        "bounds": {
          "$ref": "#/definitions/MapBoundsDto"
        },
        "center": {
          "$ref": "#/definitions/MapCenterDto"
        },
        "maxZoom": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "minZoom": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "name": {
          "description": "The archive's own name, when it declares one.",
          "type": [
            "string",
            "null"
          ]
        },
        "tileFormat": {
          "$ref": "#/definitions/MapTileFormatDto"
        },
        "tileUrlTemplate": {
          "description": "Tile URL template with `{z}`/`{x}`/`{y}` placeholders, ready to hand to\nMapLibre as a source URL. Server-relative and same-origin: tiles are\nserved by `jarvisd`, so no map traffic leaves the machine (ADR-013).",
          "type": "string"
        }
      },
      "required": [
        "bounds",
        "minZoom",
        "maxZoom",
        "center",
        "tileUrlTemplate",
        "tileFormat",
        "attribution"
      ],
      "type": "object"
    },
    "MapPointDto": {
      "description": "A single WGS84 point in degrees (F3b.5, docs/12 §3): a destination pin, the\ncurrent-location dot, or one vertex of a route polyline. Deliberately bare\n— no zoom, no bounds, no rendering hint. Those live on the coverage\nendpoint (`crate::maps::MapCoverageResponse`), which the client consults\nseparately to decide *how* to render a point, never on the point itself.",
      "properties": {
        "lat": {
          "format": "double",
          "type": "number"
        },
        "lon": {
          "format": "double",
          "type": "number"
        }
      },
      "required": [
        "lon",
        "lat"
      ],
      "type": "object"
    },
    "MapTileFormatDto": {
      "description": "What the archive's tiles are, so the client picks a vector or raster source.",
      "oneOf": [
        {
          "enum": [
            "png",
            "jpeg",
            "webp",
            "avif"
          ],
          "type": "string"
        },
        {
          "const": "mvt",
          "description": "Mapbox Vector Tile — the ADR-013 production case (MapLibre GL vector source).",
          "type": "string"
        }
      ]
    },
    "MediaCommandRequest": {
      "description": "`POST /api/v1/media/command` — the owner-driven transport control behind the\nmedia bar (exit evidence #4).\n\n`command` is one of the closed transport verbs (`play`, `pause`,\n`play_pause`, `stop`, `next`, `previous`, `seek`, `set_volume`); unknown\nverbs are a 400, never forwarded. Omitting `player` targets the unambiguous\nactive player and fails cleanly (409) when the choice is ambiguous — the\nserver never picks one.",
      "properties": {
        "command": {
          "type": "string"
        },
        "offsetSecs": {
          "description": "Required by `seek`; ignored otherwise. Negative rewinds.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "player": {
          "type": [
            "string",
            "null"
          ]
        },
        "volumePct": {
          "description": "Required by `set_volume`. **Must not exceed the configured cap** — the\nmedia bar cannot raise volume above it at all; going higher is the R2\napproved tool path, deliberately not reachable from a UI button\n(docs/02 §11a).",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "command"
      ],
      "type": "object"
    },
    "MediaCommandResponse": {
      "description": "Response to `POST …/media/command`: the effect that was audited and applied,\nplus the state immediately afterwards so the bar re-renders without waiting\nfor the event.",
      "properties": {
        "command": {
          "description": "The verb that was applied (echo of the request's `command`).",
          "type": "string"
        },
        "player": {
          "description": "The player it was applied to.",
          "type": "string"
        },
        "state": {
          "$ref": "#/definitions/MediaStateDto"
        }
      },
      "required": [
        "command",
        "player",
        "state"
      ],
      "type": "object"
    },
    "MediaGridItemDto": {
      "description": "One tile of a media/menu grid card (docs/12 §2.3: \"photo + name + price\").",
      "properties": {
        "name": {
          "type": "string"
        },
        "photo": {
          "anyOf": [
            {
              "$ref": "#/definitions/SourcedImageDto"
            },
            {
              "type": "null"
            }
          ]
        },
        "price": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "MediaPlayerDto": {
      "description": "One player's state on the media bar.",
      "properties": {
        "canGoNext": {
          "type": "boolean"
        },
        "canGoPrevious": {
          "type": "boolean"
        },
        "canPause": {
          "type": "boolean"
        },
        "canPlay": {
          "description": "What the player says it supports — the bar disables the rest rather than\noffering a control that will silently do nothing.",
          "type": "boolean"
        },
        "canSeek": {
          "type": "boolean"
        },
        "identity": {
          "description": "Human-readable player name (\"Spotify\"), sanitized player-published text.",
          "type": "string"
        },
        "metadata": {
          "$ref": "#/definitions/TrackMetadataDto"
        },
        "player": {
          "description": "The MPRIS bus name (`org.mpris.MediaPlayer2.spotify`) — the handle a\ncommand targets. Validated as a bus name server-side on the way back in.",
          "type": "string"
        },
        "status": {
          "$ref": "#/definitions/PlaybackStatusDto"
        },
        "volumePct": {
          "description": "Current volume in whole percent, when the player exposes one.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "player",
        "identity",
        "status",
        "metadata",
        "canPlay",
        "canPause",
        "canGoNext",
        "canGoPrevious",
        "canSeek"
      ],
      "type": "object"
    },
    "MediaStateDto": {
      "description": "Payload of the transient `media.state` event and of `GET …/media/state`.",
      "properties": {
        "activePlayer": {
          "description": "The player an untargeted command applies to, when exactly one is\nunambiguous. `null` with a non-empty `players` means the choice is\ngenuinely ambiguous (two players playing) — the bar shows both and the\nvoice path asks (ADR-016), it never guesses.",
          "type": [
            "string",
            "null"
          ]
        },
        "maxVolumePct": {
          "description": "The configured volume cap in percent. The bar clamps its own slider to\nthis; the server enforces it regardless (hearing protection, docs/02\n§11a).",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "players": {
          "description": "Players currently on the session bus, in a stable order. Empty means\nnothing is running — the bar hides itself rather than showing an empty\nshell.",
          "items": {
            "$ref": "#/definitions/MediaPlayerDto"
          },
          "type": "array"
        }
      },
      "required": [
        "players",
        "maxVolumePct"
      ],
      "type": "object"
    },
    "MediaStateResponse": {
      "description": "`GET /api/v1/media/state`. Present because `media.state` is transient and\ntherefore never replayed on resync (docs/05 §3): a client fetches the current\nvalue once on connect and follows events afterwards.",
      "properties": {
        "available": {
          "description": "False when media control is not configured or no session bus is present.\nThe bar renders nothing at all rather than dead buttons.",
          "type": "boolean"
        },
        "state": {
          "$ref": "#/definitions/MediaStateDto"
        }
      },
      "required": [
        "state",
        "available"
      ],
      "type": "object"
    },
    "MemoryDto": {
      "properties": {
        "confidence": {
          "format": "float",
          "type": "number"
        },
        "createdAt": {
          "type": "string"
        },
        "id": {
          "$ref": "#/definitions/UlidString"
        },
        "layer": {
          "$ref": "#/definitions/MemoryLayerDto"
        },
        "pinned": {
          "type": "boolean"
        },
        "retention": {
          "$ref": "#/definitions/RetentionDto"
        },
        "scope": {
          "$ref": "#/definitions/MemoryScopeDto"
        },
        "sensitivity": {
          "type": "string"
        },
        "source": {
          "$ref": "#/definitions/MemorySourceDto"
        },
        "text": {
          "type": "string"
        },
        "updatedAt": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "layer",
        "text",
        "source",
        "scope",
        "retention",
        "confidence",
        "sensitivity",
        "pinned",
        "createdAt",
        "updatedAt"
      ],
      "type": "object"
    },
    "MemoryLayerDto": {
      "enum": [
        "working",
        "episodic",
        "semantic",
        "procedural"
      ],
      "type": "string"
    },
    "MemoryListResponse": {
      "properties": {
        "memories": {
          "items": {
            "$ref": "#/definitions/MemoryDto"
          },
          "type": "array"
        },
        "nextCursor": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "memories"
      ],
      "type": "object"
    },
    "MemoryScopeDto": {
      "oneOf": [
        {
          "enum": [
            "user"
          ],
          "type": "string"
        },
        {
          "additionalProperties": false,
          "properties": {
            "session": {
              "type": "string"
            }
          },
          "required": [
            "session"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "project": {
              "type": "string"
            }
          },
          "required": [
            "project"
          ],
          "type": "object"
        }
      ]
    },
    "MemorySourceDto": {
      "properties": {
        "id": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "MessageDto": {
      "properties": {
        "content": {
          "items": {
            "$ref": "#/definitions/ContentBlock"
          },
          "type": "array"
        },
        "createdAt": {
          "description": "RFC 3339.",
          "type": "string"
        },
        "id": {
          "$ref": "#/definitions/UlidString"
        },
        "role": {
          "$ref": "#/definitions/MessageRole"
        },
        "sessionId": {
          "$ref": "#/definitions/UlidString"
        }
      },
      "required": [
        "id",
        "sessionId",
        "role",
        "content",
        "createdAt"
      ],
      "type": "object"
    },
    "MessageRole": {
      "enum": [
        "user",
        "assistant",
        "system"
      ],
      "type": "string"
    },
    "MiniStatDto": {
      "description": "One labeled value in a value-readout card's mini-stats row (docs/12 §2.3).",
      "properties": {
        "label": {
          "type": "string"
        },
        "value": {
          "description": "Display text, e.g. \"68%\" — rendered tabular-nums client-side, not\ncomputed there, so a mixed-format value never breaks alignment.",
          "type": "string"
        }
      },
      "required": [
        "label",
        "value"
      ],
      "type": "object"
    },
    "MintCapabilityTokenRequest": {
      "additionalProperties": false,
      "description": "`POST /api/v1/apps/{id}/versions/{version}/capability-tokens` — ask for a\nshort-lived, single-use token for one declared capability.",
      "properties": {
        "capability": {
          "$ref": "#/definitions/CapabilityDto"
        }
      },
      "required": [
        "capability"
      ],
      "type": "object"
    },
    "OpenArtifactRequest": {
      "description": "`POST /api/v1/artifacts/{id}/open` (FR-09/10): request that an artifact be\nrendered on a selected display. `display` names a monitor connector; when\nomitted, the server falls back to the display profile's `ArtifactCanvas`\nassignment and fails closed (409) if neither resolves a monitor.",
      "properties": {
        "display": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "OpenArtifactResponse": {
      "description": "Response to `POST …/open`: the placement that was audited and dispatched to\nthe agent. Delivery to the agent is fire-and-forget over the display channel;\na disconnected agent means the directive was audited but not yet applied.",
      "properties": {
        "artifactId": {
          "$ref": "#/definitions/UlidString"
        },
        "dispatched": {
          "description": "True when at least one display-agent device was connected to receive the\ndirective; false means audited-but-undelivered (the UI can surface this).",
          "type": "boolean"
        },
        "monitor": {
          "type": "string"
        },
        "surface": {
          "$ref": "#/definitions/SurfaceDto"
        }
      },
      "required": [
        "artifactId",
        "surface",
        "monitor",
        "dispatched"
      ],
      "type": "object"
    },
    "PairRequest": {
      "properties": {
        "deviceName": {
          "type": "string"
        },
        "pairingCode": {
          "type": "string"
        }
      },
      "required": [
        "pairingCode",
        "deviceName"
      ],
      "type": "object"
    },
    "PairResponse": {
      "properties": {
        "deviceId": {
          "$ref": "#/definitions/UlidString"
        },
        "deviceToken": {
          "description": "Opaque 256-bit bearer token; the only time it crosses the wire.",
          "type": "string"
        },
        "scopes": {
          "description": "Device scopes, e.g. `ui`, `display-agent`, `voice-capture` (docs/05 §6).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "deviceId",
        "deviceToken",
        "scopes"
      ],
      "type": "object"
    },
    "PatchMemoryRequest": {
      "properties": {
        "pinned": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "retention": {
          "anyOf": [
            {
              "$ref": "#/definitions/RetentionDto"
            },
            {
              "type": "null"
            }
          ]
        },
        "text": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PlaybackStatusDto": {
      "description": "Wire mirror of `jarvis_domain::media::PlaybackStatus`.",
      "enum": [
        "playing",
        "paused",
        "stopped"
      ],
      "type": "string"
    },
    "ProblemDetails": {
      "description": "RFC 9457 problem details body plus the stable machine `code` (docs/05 §2).\nThe gateway maps every boundary-crossing error through one `IntoProblem`\nimpl — no inline problem bodies.",
      "properties": {
        "code": {
          "$ref": "#/definitions/ErrorCode"
        },
        "detail": {
          "type": [
            "string",
            "null"
          ]
        },
        "instance": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "title": {
          "type": "string"
        },
        "type": {
          "description": "Problem type URI; `about:blank` when the code says it all.",
          "type": "string"
        }
      },
      "required": [
        "type",
        "title",
        "status",
        "code"
      ],
      "type": "object"
    },
    "PromoteListResponse": {
      "description": "`POST /api/v1/lists/{id}/promote` — the list is now a versioned markdown\nartifact (FR-08). A list promoted before gets the **next version** of the\nsame artifact, never a rival document.",
      "properties": {
        "artifactId": {
          "$ref": "#/definitions/UlidString"
        },
        "firstPromotion": {
          "description": "True when this promotion created the artifact rather than versioning it.",
          "type": "boolean"
        },
        "sha256": {
          "description": "Content address of the document, lowercase hex — the same value the\nartifact blob endpoint serves under.",
          "type": "string"
        },
        "version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "artifactId",
        "version",
        "sha256",
        "firstPromotion"
      ],
      "type": "object"
    },
    "PromoteNotesResponse": {
      "description": "`POST /api/v1/sessions/{id}/deepdive/promote` — the human accepted the\noffer; the thread is now a versioned markdown artifact (FR-08).\n\nRe-promoting the same thread appends a version to the same document rather\nthan minting a rival one, the same shape as\n[`crate::lists::PromoteListResponse`].",
      "properties": {
        "artifactId": {
          "$ref": "#/definitions/UlidString"
        },
        "firstPromotion": {
          "description": "True when this promotion created the document rather than versioning it.",
          "type": "boolean"
        },
        "sha256": {
          "description": "Content address of the document, lowercase hex.",
          "type": "string"
        },
        "version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "artifactId",
        "version",
        "sha256",
        "firstPromotion"
      ],
      "type": "object"
    },
    "ProviderDto": {
      "properties": {
        "id": {
          "description": "Profile id, e.g. `claude-cli` (docs/03 §4).",
          "type": "string"
        },
        "quota": {
          "anyOf": [
            {
              "$ref": "#/definitions/QuotaState"
            },
            {
              "type": "null"
            }
          ]
        },
        "reason": {
          "description": "Stable reason code only — never raw provider/driver text (docs/06 §5).",
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "$ref": "#/definitions/ProviderState"
        }
      },
      "required": [
        "id",
        "state"
      ],
      "type": "object"
    },
    "ProviderState": {
      "oneOf": [
        {
          "enum": [
            "healthy"
          ],
          "type": "string"
        },
        {
          "const": "degraded",
          "description": "Reachable but degraded (e.g. rate-limited); runs may queue.",
          "type": "string"
        },
        {
          "const": "unavailable",
          "description": "Unusable (quota exhausted / auth missing / network down); LLM runs queue.",
          "type": "string"
        }
      ]
    },
    "ProvidersResponse": {
      "properties": {
        "providers": {
          "items": {
            "$ref": "#/definitions/ProviderDto"
          },
          "type": "array"
        }
      },
      "required": [
        "providers"
      ],
      "type": "object"
    },
    "QuotaState": {
      "properties": {
        "resetAt": {
          "description": "RFC 3339 instant the quota/rate window resets, when the provider\nreports one — the user sees *when* service returns (docs/03 §4).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RetentionDto": {
      "oneOf": [
        {
          "enum": [
            "until_forgotten",
            "session"
          ],
          "type": "string"
        },
        {
          "additionalProperties": false,
          "properties": {
            "expires_at": {
              "type": "string"
            }
          },
          "required": [
            "expires_at"
          ],
          "type": "object"
        }
      ]
    },
    "RiskLevelDto": {
      "description": "Wire projection of [`jarvis_domain::policy::RiskLevel`] (docs/06 §3). An\napproval card only ever carries `R2`/`R3` — `R0`/`R1` auto-authorize and `R4`\nis rejected outright — but the projection is total (no `_`) so a new tier\nforces a decision here rather than silently mapping.",
      "enum": [
        "r0",
        "r1",
        "r2",
        "r3",
        "r4"
      ],
      "type": "string"
    },
    "RunAck": {
      "description": "Acknowledgement returned by `POST /sessions/{id}/messages` (docs/05 §1):\nthe run has been accepted and persisted; streaming follows on the WS.",
      "properties": {
        "runId": {
          "$ref": "#/definitions/UlidString"
        },
        "sessionId": {
          "$ref": "#/definitions/UlidString"
        },
        "state": {
          "$ref": "#/definitions/RunStateDto"
        }
      },
      "required": [
        "runId",
        "sessionId",
        "state"
      ],
      "type": "object"
    },
    "RunBudgetDto": {
      "description": "Per-run resource caps (docs/05 §4, NFR-12). Durations are seconds on the\nwire (JSON has no duration type).",
      "properties": {
        "maxArtifactBytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "maxDurationSecs": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "maxModelTurns": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "maxToolCalls": {
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "maxModelTurns",
        "maxToolCalls",
        "maxDurationSecs",
        "maxArtifactBytes"
      ],
      "type": "object"
    },
    "RunDto": {
      "properties": {
        "budget": {
          "$ref": "#/definitions/RunBudgetDto"
        },
        "createdAt": {
          "description": "RFC 3339.",
          "type": "string"
        },
        "id": {
          "$ref": "#/definitions/UlidString"
        },
        "outcome": {
          "anyOf": [
            {
              "$ref": "#/definitions/RunOutcome"
            },
            {
              "type": "null"
            }
          ],
          "description": "Present only once the run is terminal."
        },
        "sessionId": {
          "$ref": "#/definitions/UlidString"
        },
        "state": {
          "$ref": "#/definitions/RunStateDto"
        },
        "updatedAt": {
          "description": "RFC 3339.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "sessionId",
        "state",
        "budget",
        "createdAt",
        "updatedAt"
      ],
      "type": "object"
    },
    "RunOutcome": {
      "description": "Terminal outcome of a run (docs/02 §4). `detail` is a short human sentence,\nnever raw provider/driver text (docs/06 §5).",
      "properties": {
        "detail": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/definitions/RunOutcomeKind"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "RunOutcomeKind": {
      "enum": [
        "completed",
        "failed",
        "cancelled"
      ],
      "type": "string"
    },
    "RunStateDto": {
      "description": "Wire projection of `jarvis_domain::RunState` (docs/02 §4). The policy/tool/\napproval states exist on the wire from M1 but are only produced once their\nstep executors land in M2 — the shape is stable so clients need no change.",
      "enum": [
        "received",
        "context_ready",
        "model_running",
        "policy_review",
        "waiting_approval",
        "tool_running",
        "replanning",
        "responding",
        "completed",
        "failed",
        "cancelled"
      ],
      "type": "string"
    },
    "ServiceStatus": {
      "oneOf": [
        {
          "const": "ok",
          "description": "Core and all enabled adapters ready.",
          "type": "string"
        },
        {
          "const": "degraded",
          "description": "Core up, one or more adapters down — degraded mode (FR-12).",
          "type": "string"
        }
      ]
    },
    "SessionDto": {
      "properties": {
        "createdAt": {
          "description": "RFC 3339.",
          "type": "string"
        },
        "id": {
          "$ref": "#/definitions/UlidString"
        },
        "status": {
          "$ref": "#/definitions/SessionStatus"
        },
        "title": {
          "type": [
            "string",
            "null"
          ]
        },
        "updatedAt": {
          "description": "RFC 3339.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "status",
        "createdAt",
        "updatedAt"
      ],
      "type": "object"
    },
    "SessionListResponse": {
      "properties": {
        "nextCursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "sessions": {
          "items": {
            "$ref": "#/definitions/SessionDto"
          },
          "type": "array"
        }
      },
      "required": [
        "sessions"
      ],
      "type": "object"
    },
    "SessionStatus": {
      "enum": [
        "active",
        "archived"
      ],
      "type": "string"
    },
    "SourceFindingDto": {
      "description": "One page a turn consulted, as filed to `POST\n/api/v1/sessions/{id}/deepdive/findings`.\n\nBoth fields are untrusted (Z4 — they come from a fetched page). The URL is\nre-validated by `ResearchThread::record_source`, which refuses anything that\nis not an attributable `http(s)` URL, and the title is capped and escaped\nwherever it is rendered.",
      "properties": {
        "title": {
          "type": "string"
        },
        "url": {
          "type": "string"
        }
      },
      "required": [
        "title",
        "url"
      ],
      "type": "object"
    },
    "SourceHandoffDto": {
      "description": "\"Open that / let me read it\" resolved to a page the thread already cited\n(ADR-017 §3).\n\nA **citation, not a command**: `url` and `domain` are already on the sources\ncard, `domain` is computed host-side from the parsed host\n(`jarvis_domain::deepdive::display_domain`) so a spoofing URL cannot present\nitself as a trusted site, and there is no tool id here for a client to\nsubmit anywhere.",
      "properties": {
        "domain": {
          "description": "Display domain for what Jarvis says while handing off, e.g.\n\"en.wikipedia.org\".",
          "type": "string"
        },
        "url": {
          "description": "The page to open; always `http(s)`, validated when it was recorded.",
          "type": "string"
        }
      },
      "required": [
        "url",
        "domain"
      ],
      "type": "object"
    },
    "SourceItemDto": {
      "description": "One page consulted during a deep dive, as the sources card lists it\n(docs/12 §2.3: \"title + domain + link each\"; FR-27/ADR-017).\n\n`domain` is the chip label and is computed **server-side** from the parsed\nhost (`jarvis_domain::deepdive::display_domain`), never by the client from\n`url` — a `https://wikipedia.org@evil.example/` link must not be able to\npresent itself as `wikipedia.org`. Like [`SourcedImageDto`], every field is\nrequired: there is no way to list a reference without saying where it goes.",
      "properties": {
        "domain": {
          "description": "Display domain for the chip, e.g. \"en.wikipedia.org\".",
          "type": "string"
        },
        "title": {
          "description": "The page's title as consulted — plain text, never markup.",
          "type": "string"
        },
        "url": {
          "description": "The link target; always `http(s)` (validated server-side).",
          "type": "string"
        }
      },
      "required": [
        "title",
        "url",
        "domain"
      ],
      "type": "object"
    },
    "SourcedImageDto": {
      "description": "A card image sourced from the web (docs/12 §2.3, FR-25/ADR-014 — person,\nplace, weather, menu photos absent a dedicated integration). `url` is the\nimage itself; `source_url`/`source_domain` are the page it was found on and\nits chip label (e.g. \"wikipedia.org\"), computed once server-side so the\nclient never parses an untrusted URL to render trusted-looking text. `alt`\nis required alt text (docs/12 §8 accessibility) — there is no constructor\npath that produces an image without it.",
      "properties": {
        "alt": {
          "description": "Required alt text — never empty.",
          "type": "string"
        },
        "sourceDomain": {
          "description": "Display domain for the chip, e.g. \"wikipedia.org ↗\".",
          "type": "string"
        },
        "sourceUrl": {
          "description": "The page the image was found on — the source-chip's link target.",
          "type": "string"
        },
        "url": {
          "description": "The `https` image URL.",
          "type": "string"
        }
      },
      "required": [
        "url",
        "sourceUrl",
        "sourceDomain",
        "alt"
      ],
      "type": "object"
    },
    "SubmitMessageRequest": {
      "description": "Body of `POST /sessions/{id}/messages` (docs/05 §1). The idempotency key\nrides the `Idempotency-Key` header (docs/05 §2), not the body.",
      "properties": {
        "content": {
          "items": {
            "$ref": "#/definitions/ContentBlock"
          },
          "type": "array"
        }
      },
      "required": [
        "content"
      ],
      "type": "object"
    },
    "SurfaceDto": {
      "description": "A logical UI surface (docs/02 §8). Wire mirror of\n`jarvis_domain::display::Surface`; jarvisd maps between them.",
      "oneOf": [
        {
          "enum": [
            "conversation",
            "run_timeline",
            "approval_tray",
            "artifact_canvas",
            "ambient_status",
            "diagnostics"
          ],
          "type": "string"
        },
        {
          "const": "media_window",
          "description": "The credential-free media window (FR-22, ADR-012 cast-a-link).",
          "type": "string"
        }
      ]
    },
    "TimelineItem": {
      "description": "One persisted entry in a session's history.",
      "oneOf": [
        {
          "properties": {
            "message": {
              "$ref": "#/definitions/MessageDto"
            },
            "type": {
              "const": "message",
              "type": "string"
            }
          },
          "required": [
            "type",
            "message"
          ],
          "type": "object"
        },
        {
          "description": "Any persisted run event — the same [`DomainEvent`] that streamed live.",
          "properties": {
            "event": {
              "$ref": "#/definitions/DomainEvent"
            },
            "type": {
              "const": "run_event",
              "type": "string"
            }
          },
          "required": [
            "type",
            "event"
          ],
          "type": "object"
        }
      ]
    },
    "TimelineResponse": {
      "properties": {
        "items": {
          "items": {
            "$ref": "#/definitions/TimelineItem"
          },
          "type": "array"
        },
        "nextSince": {
          "description": "Sequence cursor to pass as `since` for the next page / next resync;\nabsent when the snapshot reaches the head.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "items"
      ],
      "type": "object"
    },
    "TimerActionRequest": {
      "description": "`POST /api/v1/timers/{id}/action`.\n\n`action` is one of `cancel`, `dismiss`, `snooze`. There is deliberately **no\n`fire`**: firing is something the clock does, never something a request asks\nfor — an unknown verb is a 400 and never reaches the store.",
      "properties": {
        "action": {
          "type": "string"
        },
        "snoozeSecs": {
          "description": "How long to push a ringing timer out by. Honoured by `snooze` only;\nomitted ⇒ the nine-minute default.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    },
    "TimerActionResponse": {
      "description": "The timer after the action was applied and audited, so the card re-renders\nwithout waiting for an event.",
      "properties": {
        "timer": {
          "$ref": "#/definitions/TimerDto"
        }
      },
      "required": [
        "timer"
      ],
      "type": "object"
    },
    "TimerDto": {
      "description": "One timer as the HUD renders it.",
      "properties": {
        "durationSecs": {
          "description": "The countdown's original span — present only for `countdown`, so the card\ncan show \"10 min\" alongside the live remainder.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "fireAt": {
          "description": "When it goes (or went) off, RFC 3339 UTC.",
          "type": "string"
        },
        "id": {
          "$ref": "#/definitions/UlidString"
        },
        "kind": {
          "$ref": "#/definitions/TimerKindDto"
        },
        "name": {
          "description": "Human label (\"pasta timer\"), sanitized. Rendered as text.",
          "type": "string"
        },
        "note": {
          "description": "The reminder's spoken line (\"call Mom\") — present only for `reminder`.",
          "type": [
            "string",
            "null"
          ]
        },
        "remainingSecs": {
          "description": "Seconds left **at the moment this DTO was produced**. Absent when the\ntimer is not armed (ringing or finished). The card ticks its own display\ndown from here rather than polling the server — one value, then local\narithmetic (docs/09 §5: no polling loop for something a clock can do).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "state": {
          "$ref": "#/definitions/TimerStateDto"
        }
      },
      "required": [
        "id",
        "name",
        "kind",
        "state",
        "fireAt"
      ],
      "type": "object"
    },
    "TimerKindDto": {
      "description": "Wire mirror of `jarvis_domain::timers::TimerKind`, flattened: the\nkind-specific payload rides in [`TimerDto`]'s optional `durationSecs`/`note`\nso a client can switch on one string.",
      "oneOf": [
        {
          "const": "countdown",
          "description": "\"Ten minutes from now.\"",
          "type": "string"
        },
        {
          "const": "alarm",
          "description": "\"At seven.\"",
          "type": "string"
        },
        {
          "const": "reminder",
          "description": "\"At six, to call Mom.\"",
          "type": "string"
        }
      ]
    },
    "TimerListResponse": {
      "description": "`GET /api/v1/timers` — everything outstanding, earliest first. Terminal\ntimers are not listed: \"what have I got set?\" means the live ones.",
      "properties": {
        "now": {
          "description": "Server time when the list was taken, RFC 3339. The card uses it to\ncorrect for clock skew between the browser and the daemon instead of\ntrusting `Date.now()`.",
          "type": "string"
        },
        "timers": {
          "items": {
            "$ref": "#/definitions/TimerDto"
          },
          "type": "array"
        }
      },
      "required": [
        "timers",
        "now"
      ],
      "type": "object"
    },
    "TimerStateDto": {
      "description": "Wire mirror of `jarvis_domain::timers::TimerState`. Total projection (no `_`\narm) so a new state forces a decision here rather than silently mapping.",
      "oneOf": [
        {
          "enum": [
            "pending",
            "snoozed",
            "dismissed",
            "cancelled"
          ],
          "type": "string"
        },
        {
          "const": "fired",
          "description": "Ringing, awaiting dismiss or snooze — **not** terminal.",
          "type": "string"
        }
      ]
    },
    "TrackMetadataDto": {
      "description": "Track metadata as published by the player — **untrusted display data**\n(docs/06 §2 Z4). Absent fields are omitted rather than blank so the card can\nrender \"unknown\" instead of an empty line.",
      "properties": {
        "album": {
          "type": [
            "string",
            "null"
          ]
        },
        "artUrl": {
          "description": "`https`-only album art (the domain drops every other scheme). The client\nmay render it directly; a player cannot use this field to make the shell\nfetch a local file or an internal address.",
          "type": [
            "string",
            "null"
          ]
        },
        "artist": {
          "type": [
            "string",
            "null"
          ]
        },
        "lengthSecs": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "title": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "TransientEvent": {
      "description": "Disposable, never-replayed events (docs/05 §3 \"not persisted\"). A durable\nsnapshot (`DomainEvent`) always follows the work these describe. Dotted tags\nand strict (no-catch-all) decoding for the same reasons as [`DomainEvent`].",
      "oneOf": [
        {
          "description": "One incremental chunk of streamed model output (FR-01).",
          "properties": {
            "runId": {
              "$ref": "#/definitions/UlidString"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "const": "text.delta",
              "type": "string"
            }
          },
          "required": [
            "type",
            "runId",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "A run entered degraded-mode queueing. The next durable run snapshot is\nauthoritative after reconnect; this live notice supplies the position\nwhile the provider is unavailable (FR-12, angular-shell §5).",
          "properties": {
            "position": {
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            "reason": {
              "type": "string"
            },
            "runId": {
              "$ref": "#/definitions/UlidString"
            },
            "type": {
              "const": "degraded.queued",
              "type": "string"
            }
          },
          "required": [
            "type",
            "runId",
            "reason",
            "position"
          ],
          "type": "object"
        },
        {
          "description": "Current local playback state, feeding the media bar (FR-22, docs/02\n§11a — \"a `media.state` transient WS event\").\n\nTransient is the correct classification, not a shortcut: this is a\n*current-value readout* of whatever is playing right now, not a fact\nabout the run timeline. A client that missed one is not missing history\n— it holds a stale value that the next change corrects, and a client\nthat just connected reads `GET /api/v1/media/state` instead of replaying.\nIt is also not run-scoped: media state exists with no run in flight,\nwhich is why this variant carries no `RunId`.",
          "properties": {
            "state": {
              "$ref": "#/definitions/MediaStateDto"
            },
            "type": {
              "const": "media.state",
              "type": "string"
            }
          },
          "required": [
            "type",
            "state"
          ],
          "type": "object"
        },
        {
          "description": "One canvas instruction for the HUD (F3b.6, FR-27/FR-24, ADR-017): what\nthis turn does to the materialization canvas, and the cards that belong\non it. The **first producer** of [`crate::cards::HudCardDto`] on the wire.\n\nTransient, for the same reason as `media.state` and for one more:\n\n* The canvas is a *current-value* surface, not history. Panels shelve,\n  are dismissible, and expire silently on a TTL (docs/12 §4), so a\n  client that missed an instruction is not missing a fact about the past\n  — and the payload carries the live card set rather than a delta, so\n  the next instruction re-converges it. The durable record of a thread\n  is the Research Notes artifact (FR-08), which has its own read surface.\n* A `DomainEvent` is published from the outbox in the same transaction\n  as the domain change it describes (invariant #6). A deep-dive turn\n  commits no row, so there is no transaction to ride and nothing\n  honest to replay from.\n\nNot run-scoped: a canvas update can come from a list command with no run\nand no session at all, which is why the session id lives (optional)\ninside the payload rather than beside it.",
          "properties": {
            "canvas": {
              "$ref": "#/definitions/HudCanvasDto"
            },
            "type": {
              "const": "hud.canvas",
              "type": "string"
            }
          },
          "required": [
            "type",
            "canvas"
          ],
          "type": "object"
        },
        {
          "description": "Partial or final speech recognition text on the voice channel. It is\ndisposable: the committed user message is the durable transcript.",
          "properties": {
            "final": {
              "type": "boolean"
            },
            "streamId": {
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "const": "voice.transcript",
              "type": "string"
            }
          },
          "required": [
            "type",
            "streamId",
            "text",
            "final"
          ],
          "type": "object"
        },
        {
          "description": "A voice-pipeline leg failed (F5.2). Transient for the same reason as\n`voice.transcript`: it describes a live capture/playback attempt, not a\nfact about the run timeline, and a client that was disconnected has\nnothing to recover.\n\nIt exists because a stream that simply *ends* means the service finished\nnormally (`jarvis_application::voice`) — without this event a dead STT\nservice is indistinguishable from a user who said nothing, and a dead TTS\nservice is indistinguishable from a silent response. Only a stable\n[`crate::voice::VoiceErrorCodeDto`] crosses the wire; no service text.",
          "properties": {
            "code": {
              "$ref": "#/definitions/VoiceErrorCodeDto"
            },
            "streamId": {
              "description": "The capture stream, or the utterance, the failure belongs to.",
              "type": "string"
            },
            "type": {
              "const": "voice.error",
              "type": "string"
            }
          },
          "required": [
            "type",
            "streamId",
            "code"
          ],
          "type": "object"
        }
      ]
    },
    "UiSettingsDto": {
      "description": "Non-sensitive HUD presentation settings surfaced to the paired shell.\nThese are display policy, not credentials or filesystem paths (docs/09 §1,\ndocs/12 §5–§6).",
      "properties": {
        "background": {
          "description": "`none | abstract | photo` (docs/12 §5).",
          "type": "string"
        },
        "motion": {
          "description": "`auto | reduced` (docs/12 §6).",
          "type": "string"
        },
        "panelTtlHours": {
          "description": "Silent panel expiry in hours; approvals remain exempt (docs/12 §4).",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "background",
        "panelTtlHours",
        "motion"
      ],
      "type": "object"
    },
    "UlidString": {
      "description": "ULID: 26 chars of uppercase Crockford base32",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "type": "string"
    },
    "VoiceControlDto": {
      "description": "JSON control frames that bracket a voice PCM stream.",
      "oneOf": [
        {
          "description": "Sent before the first binary PCM frame.",
          "properties": {
            "channels": {
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0,
              "type": "integer"
            },
            "sampleRateHz": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "sampleWidthBytes": {
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0,
              "type": "integer"
            },
            "sessionId": {
              "type": [
                "string",
                "null"
              ]
            },
            "streamId": {
              "type": "string"
            },
            "type": {
              "const": "voice.stream.start",
              "type": "string"
            }
          },
          "required": [
            "type",
            "streamId",
            "sampleRateHz",
            "sampleWidthBytes",
            "channels"
          ],
          "type": "object"
        },
        {
          "description": "Sent after the final binary PCM frame.",
          "properties": {
            "streamId": {
              "type": "string"
            },
            "type": {
              "const": "voice.stream.stop",
              "type": "string"
            }
          },
          "required": [
            "type",
            "streamId"
          ],
          "type": "object"
        },
        {
          "description": "Daemon → client: spoken output for one turn begins; the binary frames\nthat follow are PCM in this format, until the matching\n[`VoiceControlDto::SpeakStop`]. `utteranceId` scopes the audio so a\nbarge-in-cancelled utterance can never be confused with the next one.",
          "properties": {
            "channels": {
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0,
              "type": "integer"
            },
            "runId": {
              "description": "The run whose response is being spoken, when there is one.",
              "type": [
                "string",
                "null"
              ]
            },
            "sampleRateHz": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "sampleWidthBytes": {
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0,
              "type": "integer"
            },
            "type": {
              "const": "voice.speak.start",
              "type": "string"
            },
            "utteranceId": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "utteranceId",
            "sampleRateHz",
            "sampleWidthBytes",
            "channels"
          ],
          "type": "object"
        },
        {
          "description": "Daemon → client: no further audio belongs to `utteranceId`. `reason`\ndistinguishes a finished utterance from an interrupted or failed one —\nsilence alone would be indistinguishable between the three.",
          "properties": {
            "reason": {
              "$ref": "#/definitions/VoiceSpeakEndDto"
            },
            "type": {
              "const": "voice.speak.stop",
              "type": "string"
            },
            "utteranceId": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "utteranceId",
            "reason"
          ],
          "type": "object"
        }
      ]
    },
    "VoiceErrorCodeDto": {
      "description": "Stable machine codes for a voice-pipeline failure (`voice.error`). Free-form\nservice text is deliberately **not** on the wire: the client maps the code to\nits own message, so no adapter/transport string can reach the UI (docs/06 §5).\nAdditive only — codes are never renamed or reused.",
      "oneOf": [
        {
          "const": "voice.stt_unavailable",
          "description": "The speech-to-text service could not be reached.",
          "type": "string"
        },
        {
          "const": "voice.stt_failed",
          "description": "The speech-to-text service failed or broke the protocol mid-stream.",
          "type": "string"
        },
        {
          "const": "voice.tts_unavailable",
          "description": "The text-to-speech service could not be reached.",
          "type": "string"
        },
        {
          "const": "voice.tts_failed",
          "description": "The text-to-speech service failed or broke the protocol mid-utterance.",
          "type": "string"
        }
      ]
    },
    "VoiceSpeakEndDto": {
      "description": "Why spoken output for one utterance ended (F5.2).",
      "oneOf": [
        {
          "const": "completed",
          "description": "The whole response was synthesized and sent.",
          "type": "string"
        },
        {
          "const": "cancelled",
          "description": "Barge-in (or shutdown) cancelled synthesis mid-utterance; the client\nmust stop playback of what it has already buffered.",
          "type": "string"
        },
        {
          "const": "failed",
          "description": "The synthesizer failed; playback stops and the failure is surfaced by a\n`voice.error` transient event rather than as truncated audio.",
          "type": "string"
        }
      ]
    }
  },
  "description": "Jarvis wire contract v1 (generated — do not edit)",
  "title": "jarvis-contracts"
}
