#![deny(unsafe_code)]
//! axum host binary: REST, WS hub, auth, DI wiring, config, health (docs/02 §3).
//! Real host lands in F0.5; this stub only proves the workspace links end to end.

fn main() -> anyhow::Result<()> {
    println!("jarvisd stub — host lands in F0.5");
    Ok(())
}
