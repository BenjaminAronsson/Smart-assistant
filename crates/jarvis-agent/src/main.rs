#![deny(unsafe_code)]
//! Desktop agent binary: Hyprland IPC, window placement, PTT hotkey (docs/02 §3). M3.

fn main() -> anyhow::Result<()> {
    println!("jarvis-agent stub — desktop agent lands in M3");
    Ok(())
}
